@model CS2Cases.Models.Case
@{
    ViewData["Title"] = "Редактировать кейс";
    ViewData["ActivePage"] = "Cases";
    ViewData["Breadcrumb"] = "Контент / Кейсы / Редактировать";
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
}

<div style="max-width:640px;">
    <div class="card">
        <div class="card-title">
            <svg viewBox="0 0 24 24" fill="currentColor" width="18" height="18"><path d="M3 17.25V21h3.75L17.81 9.94l-3.75-3.75L3 17.25zM20.71 7.04c.39-.39.39-1.02 0-1.41l-2.34-2.34c-.39-.39-1.02-.39-1.41 0l-1.83 1.83 3.75 3.75 1.83-1.83z"/></svg>
            Кейс: @Model.Name
        </div>

        <form method="post">
            <input type="hidden" name="Id" value="@Model.Id" />
            <div class="form-grid">
                <div class="form-group full">
                    <label class="form-label">Название кейса *</label>
                    <input type="text" name="Name" value="@Model.Name" required />
                </div>
                <div class="form-group full">
                    <label class="form-label">Описание</label>
                    <textarea name="Description">@Model.Description</textarea>
                </div>
                <div class="form-group">
                    <label class="form-label">Цена ($) *</label>
                    <input type="number" name="Price" value="@Model.Price" step="0.01" min="0" required />
                </div>
                <div class="form-group">
                    <label class="form-label">URL изображения</label>
                    <input type="text" name="ImageUrl" value="@Model.ImageUrl" />
                </div>
            </div>

            <div style="display:flex; gap:12px; margin-top:8px;">
                <button type="submit" class="btn btn-primary">Сохранить</button>
                <a href="/Admin/Cases" class="btn btn-secondary">Отмена</a>
            </div>
        </form>
    </div>
</div>
